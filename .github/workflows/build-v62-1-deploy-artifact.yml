name: Build v62.1 Deploy Artifact

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary packaging workflow disabled."
