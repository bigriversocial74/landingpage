# North Mountain Media Portal

Source repository for the North Mountain Media Portal.

Application changes are developed on scoped branches and merged into `main` through pull requests. Production credentials, the live `config.php`, uploaded media, logs, caches, and other runtime data must not be committed.
